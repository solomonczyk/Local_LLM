# Hybrid AI Architecture Transition Plan (Local + OpenAI Director)

## 1. Цель документа

Построить гибридную AI-систему, которая:
- по качеству решений **приближается к уровню Claude Opus / Gemini Flash**
- не упирается в локальное железо (GTX 1060 5GB)
- экономит токены и деньги
- масштабируется и остаётся управляемой

Архитектурный принцип:
> **Интеллект — в облаке, объём и скорость — локально**

---

## 2. Текущее состояние системы (факты)

### 2.1 Инфраструктура
- Локальная LLM (Qwen2.5-Coder-1.5B + LoRA)
- Мультиагентная система (dev, qa, security, architect, seo, ux, director)
- RAG с KB, chunking, ballast-фильтрация
- Router с escalation + confidence
- Singleton KB cache
- GTX 1060 5GB (узкое место)

---

### 2.2 Результаты eval на реальных задачах

**Общий результат**
- Общий балл: **30 / 60 (50%)**
- Синтаксически корректный код: **10%**
- Среднее время генерации: **36.4s**

**По доменам**
| Домен       | Результат | Оценка |
|------------|----------|--------|
| Security   | 75%      | 🟢 |
| QA         | 50%      | 🟡 |
| Dev        | 44.4%    | 🟡 |
| Architect  | 38.9%    | 🟠 |

**Ключевые проблемы**
- Неправильные отступы
- Обрыв генерации кода
- Форматирование ломает исполняемость
- Логика задачи понимается, но реализация нестабильна

**Вывод**
> Локальная модель **подходит как worker**, но **не как финальный decision-maker**.

---

## 3. Архитектурное решение: гибрид

### 3.1 Роли моделей

| Роль | Где работает | Назначение |
|----|-------------|-----------|
| Worker | Локально | Генерация кода, черновики, подготовка решений |
| Reviewer | Локально | Проверка по KB, чеклистам, политикам |
| Director | OpenAI | Финальные решения, архитектура, безопасность, контроль качества |

---

### 3.2 Ключевой принцип

Director **НИКОГДА** не работает напрямую с:
- сырым кодом целиком
- длинными логами
- KB чанками

Он работает **только с агрегированным смыслом**.

---

## 4. Контракт входа для Director (обязательно)

Director получает строго структурированный вход:

```json
{
  "problem_summary": "Краткое описание задачи (5–10 строк)",
  "facts": [
    "Факт 1",
    "Факт 2", 
    "Ограничение",
    "Контекст проекта"
  ],
  "agent_summaries": {
    "dev": "…",
    "security": "…",
    "qa": "…",
    "architect": "…"
  },
  "risk_level": "low | medium | high",
  "confidence": 0.00
}
```

❗ **Никакого свободного текста. Только контракт.**

---

## 5. Когда используется OpenAI Director

### 5.1 Жёсткие триггеры (всегда Director)
- `security` / `auth` / `token` / `vuln` / `breach`
- архитектурные изменения
- миграции БД
- масштабирование
- платёжная логика
- PII / compliance

### 5.2 Мягкие триггеры (через confidence)
- если `confidence < 0.7`
- если сработало ≥ 3 домена
- если агенты противоречат друг другу

### 5.3 Когда Director НЕ нужен
- генерация тестов
- мелкие UI правки
- refactor без изменения контрактов
- подготовка документации

---

## 6. Двухступенчатый пайплайн (экономия токенов)

**Stage A — локально**
- Worker + профильные агенты
- Генерация решения
- Выявление рисков
- Саммари

**Stage B — Director (OpenAI)**
- Получает только саммари
- Принимает финальное решение
- Возвращает:
  - выводы
  - риски
  - рекомендации
  - один следующий шаг

---

## 7. Настройки генерации (обязательно)

### 7.1 Локальная модель (Worker)
```python
temperature: 0.1
top_p: 0.9
max_new_tokens: 900–1200
# enforced code fences
# stop sequences включены
```
**Цель:** убрать обрывы и кривые отступы.

### 7.2 Director (OpenAI)
```python
temperature: 0.2
max_output_tokens: 800–1200
# всегда deterministic
# формат ответа жёстко фиксирован
```

---

## 8. Безопасность и приватность

Перед отправкой в OpenAI:
- redaction `.env`, токенов, ключей
- удаление PII
- разрешены только:
  - обезличенные stacktrace
  - краткие diff-фрагменты (≤120 строк)

---

## 9. Наблюдаемость и контроль стоимости

**Обязательные метрики:**
- `calls_to_director / day`
- `director_prompt_tokens`
- `director_completion_tokens`
- `total_cost_estimate`
- `confidence_before / confidence_after`
- `downgrade / escalation events`

**Цель:**
Director вызывается редко, осознанно и оправданно

---

## 10. Failover стратегия

Если OpenAI недоступен:
- система работает локально
- ответы помечаются `confidence_low`
- явно указываются риски
- предлагается ручная проверка

---

## 11. Почему НЕ 8B локально

- Не решает проблему форматирования
- Увеличивает latency
- Ухудшает UX
- Не даёт скачка в reasoning
- Усложняет поддержку

**Архитектура > размер модели**

---

## 12. Итоговое позиционирование

- **Локальная модель** = исполнитель
- **OpenAI Director** = мозг
- **Router + confidence** = нервная система
- **KB + RAG** = память
- **Eval + метрики** = самоконтроль

---

## 13. Следующий шаг

Ввести **Director Adapter**:
- единый интерфейс вызова OpenAI
- строгий контракт входа
- логирование токенов
- защита от лишних вызовов