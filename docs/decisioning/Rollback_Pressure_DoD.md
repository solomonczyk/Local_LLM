pressure считается по окну событий

основан на rollback_outcome.status=simulated

уровни: OK (<3) / HIGH (>=3)

не валит CI (пока), только сигнал

выводится в trend отчёт

выводится в CI summary

покрыто тестом
